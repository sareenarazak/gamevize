<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Game Statistics</title>
</head>
<body>
<h1>User Game Statistics</h1>
<% if (topFiveGames.length > 0) { %>
    <ul>
        <% topFiveGames.forEach(game => { %>
            <li>
                <img src="http://media.steampowered.com/steamcommunity/public/images/apps/<%= game.appid %>/<%= game.img_icon_url %>.jpg" alt="<%= game.name %> Image">

                <%= game.name %> - <%= game.playtime_forever %> minutes played
            </li>

        <% }); %>
    </ul>
<% } else { %>
    <p>No games found for the user.</p>
<% } %>
</body>
</html>
